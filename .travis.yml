language: node_js
node_js:
  - 12.16.0

#branches:
#  only:
#  - release

cache:
  directories:
    - node_modules

sudo: true
before_install:
  - sudo apt-get install sshpass
install:
  - yarn install
  - yarn build
script:
  - bash .travis/deploy.sh
