language: node_js
node_js: v10.13.0

before_install:
  - git clone -b v2.0 https://github.com/GallenHu/Teemo.git
install:
  - yarn
script:
  - npm run build

after_success:
  - git config --global user.name "Gallen.Hu"
  - git config --global user.email "gallenhu@foxmail.com"
  - cd ./dist
  - git init
  - git add -A
  - git commit -m "Travis CI Auto Builder"
  - git checkout -b gh-pages
  - git push --force -u https://${REPO_TOKEN}@github.com/GallenHu/Teemo.git gh-pages